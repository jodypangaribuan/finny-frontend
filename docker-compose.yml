version: '3.8'

services:
  finny-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: finny-frontend
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
